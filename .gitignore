# Secrets — never commit
.env
**/.env
shared/mail/mailserver.env

# Runtime data and logs
data/
**/data/
*.log

# Editor / OS detritus
.idea/
.vscode/
.DS_Store
Thumbs.db

# Built artifacts and dependencies
node_modules/
target/
*.jar

# Local certbot scratch
core/certbot/webroot/.well-known/

# Sidecar written by scripts/install-ui.js — bootstrap.sh reads it once, then
# it's superseded by .env. Don't commit.
.install-ui-result.json

# Project design docs and brainstorm notes — local only.
# These may contain context-specific secrets, internal decisions, or PII.
# If you want to commit a redacted spec, do so under a different path.
docs/
.superpowers/
.commit-msg.tmp
