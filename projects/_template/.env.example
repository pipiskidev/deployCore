# Required for the template's docker-compose.yml.
APP_PORT=
IMAGE=

# Auto-assigned by add-project.sh (from a free port in 10000-19999). Don't
# touch unless you know it's free; use a port already in use and the
# container will fail to start.
HOST_PORT=

# Add project-specific secrets and variables below.
# Examples:
# DATABASE_URL=
# REDIS_URL=
# API_KEY=
